# Векторный QR-Генератор (PWA)

Веб-приложение (PWA) для создания чистых векторных QR-кодов.
Работает офлайн, поддерживает установку на устройство.

🔴 **Live Demo:** [Посмотреть проект в работе](https://kolesovgena.ru/qr)

---

## ⚡️ О проекте

**Проблема:** Дизайнерам (мне) и типографиям часто нужны QR-коды в векторе для печати, но существующие генераторы выдают либо растр, либо SVG с кривой геометрией.
**Решение:** Разработал PWA приложение (Offline-first) на Vanilla JS + Canvas API. Написал алгоритм, который генерирует идеальные векторные файлы без лишних точек.

### Ключевые возможности:
- 📱 **PWA:** Устанавливается как приложение, работает без интернета (Service Worker).
- 📐 **Вектор:** Генерация чистого SVG для полиграфии.
- 🎨 **Кастомизация:** Скругление углов, логотипы, цвет.
- 🚀 **Performance:** Полностью Client-Side генерация (без бэкенда).
- ✂ **Удобство:** Сокращение ссылок через API Яндекса

## 🛠 Технический стек

*   **Core:** Vanilla JavaScript (ES6+), HTML5, CSS3.
*   **Rendering:** HTML5 Canvas API (для растра) + SVG генерация.
*   **Libraries:**
    *   `qrcode.js` — математика генерации матрицы кода.
    *   `opentype.js` — работа со шрифтами.
    *   `cropper.js` — кадрирование логотипов.
    *   `clipper.js` — булевы операции с полигонами (для скругления углов).

---

## 📂 Структура файлов и Навигация

В репозитории находятся как исходные файлы для разработки, так и оптимизированная сборка для продакшена.

### 🔹 Где смотреть код (Source):
Для ревью кода рекомендую смотреть эти файлы (они содержат комментарии и исходное форматирование):
*   📄 **Логика приложения:** [`main.js`](./main.js)
*   🎨 **Стили:** [`style.css`](./style.css)

### 🔸 Продакшн (Build):
В `index.html` для повышения производительности подключены минифицированные версии:
*   `main.min.js`
*   `style.min.css`

---

## 🚀 Установка и запуск

Проект статический, не требует установки зависимостей (npm/yarn).

1. Клонировать репозиторий:
```bash
git clone https://github.com/GronArkh/perfect-qr-generator.git
